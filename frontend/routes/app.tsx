import { Routes, Route, BrowserRouter } from "react-router-dom";
import './App.scss';

//Routes pages


export const App = () => {
  return (
    <BrowserRouter>
      <Routes>
      </Routes>
    </BrowserRouter>
  );
};
